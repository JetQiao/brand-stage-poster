---
name: brand-stage-poster
description: 创建或修改极简品牌舞台海报，使用巨型品牌字、克制灯光和反射地面；支持参考图、团队人物、自由度与局部锁定，以及现有海报的尺寸导出。
metadata:
  version: "1.0.0"
  language: "zh-CN"
---

# 品牌舞台海报

把品牌做成一场克制的舞台亮相。用户用一句自然语言描述主题、文字和主体即可；从已有对话与素材整理参数，不要求用户填写配置文件。

> 用品牌舞台海报风格，主字 NOVA LAB，不要人物，红黑，横版。

需要直观风格基准时查看 [公开示例](assets/preview.png)，仅参考视觉语言，不继承示例中的文字或主体。

## 默认体验

- 新建一张横版 16:9 海报，平衡档／自由度 50，红黑配色、巨型窄体品牌字、简洁舞台、反射地面。使用者给定的比例、张数、颜色和主题优先；编辑与导出继承原图比例。
- 不主动添加人物；需要真实团队时使用本次可访问的人物照片。没有图形 Logo 时使用给定的品牌文字，不发明已有品牌的标志。
- 文案只来自用户要求；不默认任何公司名称、人数、服装或修容要求。未要求换装时保留人物原有衣着。
- 使用宿主当前提供的图像生成／编辑工具，遵循其实际参数、传图和输出规则。本技能不附带模型、账号、额度或 API key；不自动切换第三方服务。
- 没有生成能力时交付提示词与素材说明，明确图片尚未生成。只有尺寸需求时使用本地导出脚本，无需生成工具。

## 先确定任务，再按需读取

| 本次请求 | 行动与参考 |
| --- | --- |
| 新建海报 | 读 [风格与控制](references/style-and-controls.md)；按 [制作流程](references/workflows.md) 的新建分支执行 |
| 更改风格、自由度、锁定项 | 读 [风格与控制](references/style-and-controls.md)；有指定底图时仍走编辑分支 |
| 修改现有海报，包括换装、文字、配色 | 读 [制作流程](references/workflows.md) 的编辑分支；现有图作为编辑底图 |
| 含真人、Logo，或要求精确文字 | 读 [制作流程](references/workflows.md) 的素材与保真部分 |
| 需要形成或修订生成指令 | 使用 [提示词配方](references/prompt-recipes.md)，只选本次适用的配方 |
| 图片生成或修改完成 | 按 [质量检查](references/quality-check.md) 核验整体及关键细节 |
| 仅导出尺寸 | 直接按 [制作流程](references/workflows.md) 的导出分支运行脚本，不加载生成配方 |

## 关键执行规则

1. **只补关键缺失。** 先查看实际素材。需要品牌海报却没有品牌文字或可辨识品牌内容，集中询问品牌内容；需要真实团队却没有照片，索取人物素材；局部编辑缺底图时索取底图。无人物、无图形 Logo 或明确无字的任务无需这些素材。
2. **分清输入职责。** 人物原照用于外观，风格参考用于视觉表达，原始 Logo 用于品牌结构，待修改图片用于编辑底图。同一图片可以承担明确标注的多种职责；不从风格图默认继承陌生品牌或人物。
3. **首次创建可以直接使用参考照片生成。** 不把新建任务误判成必须先有海报。修改现有主体或服装可以使用宿主内置编辑能力；减少对无关区域和面部的重绘。
4. **自由度与锁定独立。** 严格 0–30、平衡 31–70、探索 71–100，仅改变未锁定设计项。数值是提示词规则的分档，不是模型参数或保真百分比。用户明确的局部修改更新相应锁定，其余锁定继续生效。
5. **准确性要看结果。** 逐字核对文案，检查人数、顺序、外观、Logo 和编辑范围；不把提示词里的“锁脸”当成像素保护。默认最多两轮定向修正，同一错误仍持续时说明未通过项并调整路径；用户的次数与时间限制优先。
6. **任务产物放在工作输出目录。** 在当前项目或用户指定位置的 `output/<task-id>/` 保存 `brief.json`、`prompt.md`、`qa-report.md` 和图像；仅导出不需要生成提示词。不要把用户照片、任务记录或生成结果写入公共 Skill 目录。工具先保存到别处时，按宿主规则将用于项目的最终图复制到工作输出目录并记录路径，保留原始输出；纯预览按宿主要求展示即可。

## 交付

展示实际生成的图片或可访问文件，简短说明检查发现的偏差及真实尺寸。只在需要时导出长边 4096 或 UHD；不得把重采样图称为原生 4K。最终消息服从宿主图像工具的交付规则。

真人相似度由用户对照原片判断；不编造相似度分数，不承诺每次完全保留脸部、标志或未编辑像素。这个 Skill 提供制作规则与检查流程，不是专门训练的图像模型。
